<rule_activation id="03a98066-5f26-4bd7-9a5a-e3ee3b308413" title="Standardize Field Input Hooks Pattern for Record Field UI Components: Each Field Type" applies_to="packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/**">
These rules are ALWAYS ACTIVE for all field input components in the twenty-front package, specifically within the record-field UI meta-types input module.
</rule_activation>

### Rules

- **R-FIH-001** MUST: Each field type (Files, RelationOneToMany, RelationFromMany, MorphRelationManyToOne, MorphRelationOneToMany, RelationToOne) MUST have a corresponding dedicated hook.
- **R-FIH-002** MUST: Follow the naming convention `useOpen[FieldType]FieldInput` for hooks and `[FieldType]FieldInput` for components.
- **R-FIH-003** MUST: Place hooks in `packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/hooks/` and components in the parallel `components/` directory.
- **R-FIH-004** MUST: Ensure hooks return consistent interface patterns (e.g., open handlers, state values, callbacks) to maintain API predictability across field types.
- **R-FIH-005** SHOULD: When creating a new field input type, start by copying an existing hook (e.g., `useOpenFilesFieldInput.tsx`) as a template and adapt it to the new field type's requirements.

### Verify

```bash
# Verify hook naming convention compliance
find packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/hooks -name 'useOpen*FieldInput.tsx' | wc -l

# Verify hooks are used in components
grep -r 'useOpen.*FieldInput' packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/components/ | wc -l

# Verify directory structure
test -d packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/hooks && test -d packages/twenty-front/src/modules/object-record/record-field/ui/meta-types/input/components && echo 'Structure valid'
```

**Accept when:**
- All field input hooks follow the `useOpen[FieldType]FieldInput` naming pattern and are located in the hooks directory
- Each field input component in the components directory uses its corresponding dedicated hook
- The directory structure maintains separation between `hooks/` and `components/` within the meta-types/input module
- All field types (Files, RelationOneToMany, RelationFromMany, MorphRelationManyToOne, MorphRelationOneToMany, RelationToOne) have dedicated hooks

<enforcement>
Claude Code MUST verify all rules in this file before approving field input component implementations. Violations result in CI build warnings and code review rejection. Exceptions require Frontend Architecture Team approval and must be documented with exception ID references.
</enforcement>