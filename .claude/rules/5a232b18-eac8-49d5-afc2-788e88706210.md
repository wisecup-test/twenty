<rule_activation id="5a232b18-eac8-49d5-afc2-788e88706210" title="Standardize Public API Contract Definitions with TypeScript Type Exports: Internal Implementation Types" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching public API packages, integration packages (twenty-zapier), and public website components (twenty-website-new).
</rule_activation>

### Rules

- **R-API-001** MUST NOT: Internal implementation types MUST NOT be directly exposed as public API contracts without explicit export declarations.

### Verify

```bash
# Count exported types in dedicated .types.ts files
grep -r "export.*type.*" packages/twenty-zapier/src --include="*.types.ts" | wc -l

# Find all .types.ts files with exports across packages
find packages/*/src -name "*.types.ts" -type f | xargs grep -l "export" | wc -l

# Verify TypeScript compilation succeeds for public API packages
npx tsc --noEmit --project packages/twenty-zapier/tsconfig.json
```

**Accept when:**
- All integration packages contain at least one `*.types.ts` file with exported type definitions
- TypeScript compilation succeeds without errors for all public API packages
- Grep commands identify exported types in dedicated type definition files across integration packages
- No internal implementation types are exported directly from public API entry points
- Type definitions are separated into `*.types.ts` files distinct from `*.internal.ts` implementation files

<enforcement>
Claude Code MUST NOT skip or defer verification. All public API packages MUST pass TypeScript compilation and type export validation before acceptance. Violations block pull requests until remediated.
</enforcement>