<rule_activation id="3194d415-2c10-4484-b925-505c1c4cb15a" title="Implement Cache Layer for External API Relationship Data: Relationship Persistence Operations" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching external API integrations that expose relational data models, frontend components that render or manipulate relationship data, API client libraries, and data persistence hooks.
</rule_activation>

### Rules

- **R-CACHE-001** MUST: Relationship persistence operations MUST update or invalidate relevant cache entries to maintain consistency

### Verify

```bash
# Detect cache-relationship patterns in TypeScript files
grep -r 'cache.*relationship\|relationship.*cache' --include='*.ts' --include='*.tsx' packages/

# Search for cache layer and relationship persistence hooks
grep -r 'useMorphPersist\|cache_layer\|cacheRelation' --include='*.ts' --include='*.tsx' packages/

# Find cache implementation files with relationship handling
find . -type f -name '*cache*.ts' -o -name '*Cache*.ts' | xargs grep -l 'relationship\|relation'
```

**Accept when:**
- Cache layer implementation is detected in API integration modules with relationship handling logic
- Cache invalidation logic is present alongside relationship persistence operations
- Monitoring or metrics collection for cache performance is implemented or documented

<enforcement>
Claude Code MUST verify that relationship persistence operations include cache update or invalidation logic. Verification is mandatory before accepting external API integration implementations with relational data models.
</enforcement>